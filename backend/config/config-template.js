// Template for config.js
// Use this template to indicate changes in cinfig file, but provide only non-sensitive values ex. password: "mypassword"
"use-strict";

const env = 'dev';

const dev = {

}

const test = {

}

const prod = {

}


const config = { dev, test, prod };
// Uncomment the line below in real config.js file
// module.exports = config[env];
